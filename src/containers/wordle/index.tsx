import WordlePage from "@/pages/wordle";
// this way of writing is for desktop and mobile layouts, in this project we only have considered one
export const WordleContainer = () => {
  return <WordlePage />;
};
